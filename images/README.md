# Images

Drop production photos here, then replace the labeled `.img-placeholder` divs
in the HTML with `<img>` tags pointing to these files. Keep the dark overlay
(`rgba(30, 33, 43, 0.5)`) on photographic backgrounds for palette cohesion.

Assets referenced by the build:

| File | Used on | Notes |
|------|---------|-------|
| `og-image.jpg` | All pages (Open Graph / social share) | 1200×630, branded |
| Hero background | `index.html` | Gym action shot, dark/energetic |
| Studio interior | `about.html` | Open training floor |
| Private gym detail | `about.html` | Equipment / coaching in action |
| Kylie Pace headshot | `index.html`, `about.html` | 4:5 portrait |
| Katherine Watson headshot | `index.html`, `about.html` | 4:5 portrait |
| Todd Reeves headshot | `index.html`, `about.html` | 4:5 portrait |

Recommended: export at 2× for retina, compress (WebP or optimized JPG), and add
descriptive `alt` text when swapping in the real `<img>` tags.
